# benable-brand-prototype-v33 — Campaign Pulse

Brand-portal prototype: captured production HTML + React overlays. v27 archives the full A–W variant exploration; v28 carries only the surviving direction.

## Architecture
- Captured page HTML lives in `src/data/capturedHtml.js` (huge — grep, never read whole).
- Pages render captured HTML via `dangerouslySetInnerHTML`, then mount React overlays into injected host divs with their own `createRoot` + a MutationObserver re-mount (pattern in `CampaignDetailPage.jsx` / `CampaignsListPage.jsx`).
- Campaign page overlay: `src/components/pulse/` —
  - `amine.jsx` + A · Amine (DEFAULT) — the designer's cohort-funnel page (repo AmineBenjil/benable-cohort-funnel, Figma 11603:48887) rebuilt on v32's states. Stat row ("N% through" green + 🚀 forecast from PCT/FCAST), proportional funnel bar (36px, 3px gaps, 100px outer corners, 7-stage green ramp #d8efe2→#0d4830 w/ his AA ink fixes, empty stage = 14px hatched sliver, amber needs-you badge, dark hover tooltip w/ names), chip row (All creators / 7 stages / Needs you / Casting…), creators table card (16px radius, #fafafa column strip, verified badge, 7 purple stage dashes, rows expand to cp-hist timeline, ⚑ orange flag rows, "＋ Request more" footer), right rail 370px (While away / Up next / The pace w/ #815aff & #c4c4c4 meters + pace-strip.jpg caption strip). Casting renders as a LEADING hatched block + dashed rule (his "Exited" treatment mirrored — his funnel has no casting stage). Everything derives from CREW via `amFunnel`; bar/chips/table share v32's stageFilter (adds 'needs'). Assets in `public/labs/` are his Figma exports. CSS `am-` section in pulse.css.
  - B · Amine rail — his V2 (Figma 11638:139353): same page as A but the bar is the equal-width stage rail (40px slabs, 4px gaps, 74/100px outer corners, his V2 ramp #b9dfcb→#124a33 w/ contrast fixes, label 12.5/600 + 11.5 hint in a fixed 49px block) and there is NO chip row — the amber badge is a button doing the Needs-you filter, columns click-to-filter. Empty stage = hatched slab w/ muted 0. The frame's leading "Casting…" column (#dbeee3) — which Amine dropped for lack of a casting state — is drawn here since v32 has one. `AmineRailBar`/`AmineProgress2` in amine.jsx, `am2-` CSS.
  - C · No banner — the Amine rail (his V2) exactly, minus the needs-you/all-clear banner (Julia, Jul 27): the amber badge carries the attention signal; day-3 Review CTA (banner-hosted) goes with it. Katie's day-1 welcome card stays.
  - Switcher pruned to **A / C / W / Y / Z / 0** (Julia, Jul 27). B/V/X/P/Q/R/S/T/U/6/7/13 removed from VARIANTS only — their components remain in the repo (Y still uses PipelineSlabBar, C uses AmineProgress2) and the full lineup is in git history/v31.
  - `pulseData.js` — ALL demo content (DAYS day-states, CREW per-day rows, TIMELINES, banners). Copy tweaks go here.
  - `LiveStatus.jsx` — motion registers: shimmer = machine working now · katie = human present (typing, never a spinner) · heartbeat = watching (breathe, one still sentence) · celebrate = go-live (emoji bounces, words still) · facts/static = quiet. Every animation is a claim — only emit from real signals in production.
  - `tiles.jsx` — Lead, RecapTile ("While you were away"), UpNextTile, PaceTile, LiveBarTile (only exists after first live post; continuous fill).
  - `CampaignPulse.jsx` — crew view shell + demo scrubber. Variants: V gray+crew · W +live bar · X +call fixes (D1 Katie welcome, D3 big review CTA / orange ready rows / request-more) · P pipeline (default).
  - `labs.jsx` + Y · Labs skin (default) — Figma "Design Labs" node 11603-48887 (fileKey 8rB3KQsQhmJrnQk3EFkcDx): U's tracking bar on top; below, Figma card system — bg #f9fafb on the FULL pane below the tabs (`cp-labs-pane` on the stage shell, !important to beat captured-page cascade; tabs strip stays white via negative-margin full-bleed), white 12px-radius cards w/ #e3e3e3 borders + icon-chip headers (#f5f3fc), real Creators TABLE (CREATOR/LATEST UPDATE/STAGE cols, #fafafa header strip, verified badge #0c8ee9, purple stage dashes, always-on "＋ Request more", rows expand to the cp-hist timeline), Labs versions of While-you-were-away/Up next/The pace (lavender caption pill). Table LEFT, 344px rail RIGHT (flipped vs other variants). `.lb-li p` pins color — captured-page CSS colors bare <p> maroon.
  - Z · Gradient bar (default) = Labs skin (Y) with the bar swapped per mock: `PipelineGradientBar` — all 8 stages always visible as touching slabs on an 8-shade green ramp (pg-s0…s7), casting slot pill-rounded left, count inside every slab, zero stages GREY (#f1f1f3 — Julia: "when it's 0 the state is grey not green"), labels+captions below with no underlines/track; keeps click-to-filter + tooltips; bigger amber badge (22px). In Y/Z the .pp bar card is transparent — bar sits directly on the grey pane (Julia).
  - `pipelineBar.jsx` — P + Q + R + S + T/U. T · Slabs (default) = designer-mock look: every stage a full-width slab (pale grey "0" slab when empty — no hollow nodes), track shows only in the grid gaps as connectors, moved-forward pill in header; keeps S's click-to-filter + tooltips. U · Green ramp = same component (`PipelineSlabBar palette="green" seeall`), pu-s*/pu-l* light→dark greens replace the violet ramp (amber needs-you unchanged); seeall prop swaps the moved pill for the "See all creators" filter-clear button. S · P + filters (default) = the P/Q mash-up: P's shape UNTOUCHED (fluid full-width columns, casting column only while casting) with occupied stops as buttons that filter the crew table (shared stageFilter state w/ Q) and Q's dark hover tooltip (names, % reached, click hint; tooltip centers via own pq-tip-in keyframes — cp-swap would wipe the translateX). Header right = "See all creators" button (clears the filter; replaced the moved-forward pill per Julia). Julia rejected an R-shaped S — keep P's UI shape. R · Fixed stages: P's subway with constant geometry — always 8 slots (Casting… slot never disappears; hollow node + "none needed right now" when idle) and fixed-size stop pills (132px cap, centered), so scrubbing days never reflows. Q · Filter bar (default): "% through" progress band — filled portion splits into proportional stage segments, hatched grey = road ahead; stage chips below FILTER the crew table (state in CampaignPulse, resets on day/variant change); dark hover tooltips (names + "% reached this stage or beyond"); amber whole-stage/badge rules shared with P. P's "Where your N creators are" card: aligned stage columns on a subway track (solid stop = creators there, hollow node = future), color = who has the ball (purple ramp = in motion with us, amber = waiting on brand [whole stage, or corner badge when partial], green = content real, dashed ghost = casting). Counts derive from CREW; "moved forward" pill diffs stages vs the previous demo day.
- Brand overview overlay: `BrandPulse.jsx` (lifetime totals, milestone tracker, insight cards) mounted before `.campaigns-section`.
- CSS: `src/styles/pulse.css` only — `cp-` campaign page, `bp-` brand overview. Keep it pruned; don't append dead styles.

## Copy rules
- Operational claims say "Katie's team" (never solo Katie); Katie's first-person voice only in her signed cursive notes.
- No struggle updates; rematches framed as reassurance. Emoji stripped in crew statuses except celebrate.
- A tile row never shows a zero — it shows a sentence about what's happening.

## Dev + ship
- Dev: launch.json name `brand-prototype-v33`, port 5211. Demo page: `/brand/tonypikora/campaigns/46` (campaign) and `/brand/tonypikora/campaigns` (brand overview).
- Deploy: `bash scripts/ship.sh "commit message"` — builds, commits, pushes, watches the Pages run, curls the live URL.
- Live: https://juliabenable.github.io/benable-brand-prototype-v33/
